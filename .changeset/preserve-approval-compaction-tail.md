---
"eve": patch
---

Preserve pending tool approvals at the end of model history when compaction replays the task or restores framework state, so approved tools execute before the conversation resumes.
